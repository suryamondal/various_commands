\section{Reproducibility}
\label{app:repro}

\subsection{Environment}

Every measurement in this note comes from the pool described in
\S\ref{sec:intro}. The relevant versions and shapes:

\begin{table}[H]
\centering
\begin{tabularx}{\textwidth}{L{0.30\textwidth} Y}
\toprule
\textbf{Item} & \textbf{Value} \\
\midrule
HTCondor & 23.4.0, distribution packages, identical on every host \\
Operating system & Ubuntu 24.04 LTS on x86\_64; Debian 13 on the edge SBC \\
Entry node & 4 cores, 16\,GB, NVMe; collector, negotiator, schedd, limited startd \\
Flagship worker & 16 threads, 31\,GB; the machine in \S\ref{sec:ledger} \\
Desktops & 4 cores, 16\,GB, in daily use by their owners \\
Network & Two subnets with no route between them, both reachable by wired and wireless \\
Name resolution & mDNS, answered per interface; no DNS entries, no hosts files \\
Transport & A single TCP port via \kn{condor\_shared\_port} \\
Slot model & One partitionable slot per machine, \kn{cpus=100\%, mem=100\%, disk=100\%} \\
Enforcement & \kn{CGROUP\_MEMORY\_LIMIT\_POLICY = none}; \kn{JOB\_RENICE\_INCREMENT = 10} \\
Cadence & \kn{UPDATE\_INTERVAL} 30\,s; \kn{STARTD\_CRON} period 15\,s; \kn{POLLING\_INTERVAL} 5\,s \\
\bottomrule
\end{tabularx}
\caption{Environment for all reported measurements.}
\end{table}

The measured attributes are supplied by a \kn{STARTD\_CRON} job that prints
\kn{name = value} lines on stdout, which the startd merges into the machine
ClassAd. HTCondor publishes no free-memory attribute of its own, so
\kn{HostMemAvailMB}, \kn{HostCpuPsiAvg10} and the rest exist only because that
job reads \file{/proc/meminfo} and \file{/proc/pressure/*}. Without it the
configuration still parses and the policy is silently inert
(\S\ref{sec:silent}).


The checks that discriminate between a working policy and a dead one. Ordered by
how often they were the one that mattered.

\subsection{Is the policy actually live?}

\begin{lstlisting}[style=term]
condor_status -af Name Start OwnerSessionActive HostMemAvailMB HostDiskAvailMB
\end{lstlisting}

Reads the \textbf{slot ad}. If a policy attribute is missing here, the policy is
dead no matter what the configuration says. Allow one update interval, or force
a fresh ad with \kn{condor\_reconfig}.

\subsection{Why is this machine not taking work?}

\begin{lstlisting}[style=term]
condor_status -af Name Start TotalLoadAvg HostCpuPsiAvg10 \
                       HostMemAvailMB HostDiskAvailMB
\end{lstlisting}

Every term in \kn{START} beside its threshold. A machine can show free slots and
still decline work. That is the design, not a fault.

\subsection{What did the startd actually decide?}

\begin{lstlisting}[style=term]
grep -iE 'suspend|continue|changing activity|changing state' \
     /var/log/condor/StartLog | tail -25
\end{lstlisting}

The only place claim refusals are recorded. \kn{Request to claim resource
refused} and \kn{Claiming protocol failed} appear here and nowhere else.

\subsection{Is the pool intact?}

\begin{lstlisting}[style=term]
condor_status -af Name State Activity TotalLoadAvg HostMemAvailMB Start
condor_q -totals
\end{lstlisting}

\subsection{Did the configuration take?}

\begin{lstlisting}[style=term]
condor_config_val -v <KNOB>
\end{lstlisting}

\begin{trap}
Necessary but far from sufficient. Every defect in \S\ref{sec:silent}
printed a correct value here. The \kn{-v} flag at least reveals \emph{which} file
in \file{config.d} won, which is its real use, because precedence bites.
\end{trap}

\subsection{Observing under load}

\begin{lstlisting}[style=term]
cut -d' ' -f1 /proc/loadavg          # the real figure, not the collector's
awk '/^MemAvailable:/ {print int($2/1024)" MB"}' /proc/meminfo
cat /proc/pressure/cpu /proc/pressure/memory
\end{lstlisting}

\begin{trap}
Do not use \kn{condor\_status} to observe a stress test: it reads the collector
and is only as fresh as \kn{UPDATE\_INTERVAL}. It showed a frozen value for two
and a half minutes while load was driven from 1 to 15.

\kn{condor\_status -direct} is not an alternative on a worker, because it needs
the pool signing key to verify the caller's token and only the entry node has
it.
\end{trap}

\subsection{Load generation}

Both generators must be self-terminating, with \kn{timeout} owning the deadline
so nothing survives a dropped session.

\begin{lstlisting}[style=term]
%% CPU: tight loop, no fork per iteration
timeout "$SECS" sh -c 'while :; do :; done'

%% Memory: touch one byte per page so the pages are genuinely resident
timeout "$SECS" python3 -c '
import sys, time
mb = int(sys.argv[1])
buf = bytearray(mb * 1024 * 1024)
for i in range(0, len(buf), 4096):
    buf[i] = 1
time.sleep(int(sys.argv[2]))
' "$MB" "$SECS"
\end{lstlisting}

An untouched allocation is virtual only, and \kn{MemAvailable} would not move,
so the test would appear to fail while never having applied any pressure.

\subsection{Testing eviction without endangering the machine}

Raise the floor to meet the memory rather than driving the machine into genuine
pressure. Override \textbf{both} expressions:

\begin{lstlisting}
PREEMPT      = (HostMemAvailMB < <just under current availability>)
WANT_SUSPEND = !(HostMemAvailMB < <the same value>)
\end{lstlisting}

Overriding \kn{PREEMPT} alone leaves \kn{WANT\_SUSPEND} on the real floor, where
it stays true, so \kn{SUSPEND} is consulted instead and the test fails for the
wrong reason.
